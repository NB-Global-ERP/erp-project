## Сборка и запуск
**1. Клонируем репозиторий**
```bash
git submodule update --remote --recursive
```

**2. Собираем и запускаем докер**
   ## Общая команда
   ```bash
   docker-compose up -d
   ```

**3. Запуск браузере для просмотра**
   ```bash
   http://localhost:3000
   ```
